<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.TunisStore"
        tools:targetApi="31"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".CompteBienCreeActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".MainActivity"/>
        <activity android:name=".Bienvenue" />
        <activity android:name=".CreationCompteActivity" />
        <activity android:name=".CreationCompte2Activity" />
        <activity android:name=".ValidationChoixPreferenceActivity" />
        <activity android:name=".SeConnecter" />
        <activity android:name=".AccueilActivity" />
        <activity android:name=".CategorieActivity" />
        <activity android:name=".AchatActivity" />
        <activity android:name=".RechercheActivity" />

    </application>
</manifest>